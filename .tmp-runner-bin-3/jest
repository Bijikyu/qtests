#!/usr/bin/env node
try {
  const fs = require('fs');
  const path = require('path');
  fs.writeFileSync(path.join(process.cwd(), 'runner-jest-args-3.json'), JSON.stringify(process.argv.slice(2)), 'utf8');
} catch (e) { }
process.exit(1);
