/** Verification Helper Functions */
import type{SinonSpy,SinonStub}from'sinon';
export const verifyCallCount=(data:{spy:sinon.SinonSpy|sinon.SinonStub,count:number}):{matches:boolean}=>({matches:data.spy.callCount===data.count});export const verifyCalledWith=(data:{spy:sinon.SinonSpy|sinon.SinonStub,args:any[]}):{matches:boolean}=>({matches:data.spy.calledWith(...data.args)});export const verifyCalledOnce=(data:{spy:sinon.SinonSpy|sinon.SinonStub}):{matches:boolean}=>({matches:data.spy.calledOnce});