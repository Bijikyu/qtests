// Generated unit test for stubMethod.ts - TypeScript ES module
// 🚩AI: ENTRY_POINT_FOR_GENERATED_TEST_IMPORTS
import 'qtests/setup';

let testModule: any;
beforeAll(async () => {
  testModule = await import('./stubMethod');
});

describe('getSinonLibrary', () => {
  it('is defined', () => {
    const target = (testModule as any)['getSinonLibrary'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('verifyCallCount', () => {
  it('is defined', () => {
    const target = (testModule as any)['verifyCallCount'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('verifyCalledWith', () => {
  it('is defined', () => {
    const target = (testModule as any)['verifyCalledWith'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('verifyCalledOnce', () => {
  it('is defined', () => {
    const target = (testModule as any)['verifyCalledOnce'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('createFakeServer', () => {
  it('is defined', () => {
    const target = (testModule as any)['createFakeServer'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('createFakeXHR', () => {
  it('is defined', () => {
    const target = (testModule as any)['createFakeXHR'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('stubMethod', () => {
  it('is defined', () => {
    const target = (testModule as any)['stubMethod'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('stubUtilities', () => {
  it('is defined', () => {
    const target = (testModule as any)['stubUtilities'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('stub', () => {
  it('is defined', () => {
    const target = (testModule as any)['stub'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('spy', () => {
  it('is defined', () => {
    const target = (testModule as any)['spy'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('mock', () => {
  it('is defined', () => {
    const target = (testModule as any)['mock'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('fake', () => {
  it('is defined', () => {
    const target = (testModule as any)['fake'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('useFakeTimers', () => {
  it('is defined', () => {
    const target = (testModule as any)['useFakeTimers'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('restore', () => {
  it('is defined', () => {
    const target = (testModule as any)['restore'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('sinon', () => {
  it('is defined', () => {
    const target = (testModule as any)['sinon'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});
