// Generated unit test for mockConsole.ts - TypeScript ES module
// 🚩AI: ENTRY_POINT_FOR_GENERATED_TEST_IMPORTS
import 'qtests/setup';

let testModule: any;
beforeAll(async () => {
  testModule = await import('./mockConsole');
});

describe('mockConsole', () => {
  it('is defined', () => {
    const target = (testModule as any)['mockConsole'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('consoleMocking', () => {
  it('is defined', () => {
    const target = (testModule as any)['consoleMocking'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('ConsoleMethod', () => {
  it('is defined', () => {
    const target = (testModule as any)['ConsoleMethod'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('JestSpy', () => {
  it('is defined', () => {
    const target = (testModule as any)['JestSpy'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('ConsoleMockOptions', () => {
  it('is defined', () => {
    const target = (testModule as any)['ConsoleMockOptions'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('tryCreateJestSpy', () => {
  it('is defined', () => {
    const target = (testModule as any)['tryCreateJestSpy'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('mockConsoleWithJest', () => {
  it('is defined', () => {
    const target = (testModule as any)['mockConsoleWithJest'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('FallbackMock', () => {
  it('is defined', () => {
    const target = (testModule as any)['FallbackMock'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('createFallbackMock', () => {
  it('is defined', () => {
    const target = (testModule as any)['createFallbackMock'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('mockConsoleWithFallback', () => {
  it('is defined', () => {
    const target = (testModule as any)['mockConsoleWithFallback'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('MockSpy', () => {
  it('is defined', () => {
    const target = (testModule as any)['MockSpy'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('withMockConsole', () => {
  it('is defined', () => {
    const target = (testModule as any)['withMockConsole'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('mockAllConsole', () => {
  it('is defined', () => {
    const target = (testModule as any)['mockAllConsole'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('withAllMockedConsole', () => {
  it('is defined', () => {
    const target = (testModule as any)['withAllMockedConsole'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('isMocked', () => {
  it('is defined', () => {
    const target = (testModule as any)['isMocked'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('restoreMock', () => {
  it('is defined', () => {
    const target = (testModule as any)['restoreMock'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('restoreAllMocks', () => {
  it('is defined', () => {
    const target = (testModule as any)['restoreAllMocks'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});
