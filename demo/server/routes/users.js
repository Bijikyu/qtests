const express=require('express');const qerrors=require('qerrors');const router=express.Router();
let users=[{id:1,name:'John Doe',email:'john@example.com'},{id:2,name:'Jane Smith',email:'jane@example.com'},{id:3,name:'Bob Johnson',email:'bob@example.com'}];let nextId=4;
router.get('/users/:id',(req,res,next)=>{try{const{id}=req.params;if(!/^[1-9]\d*$/.test(id))return res.status(400).json({error:'Invalid user ID format'});const userId=parseInt(id);const user=users.find(u=>u.id===userId);if(!user)return res.status(404).json({error:'User not found'});res.json(user);}catch(error){qerrors(error,'users.get: retrieving user by ID',{userId:req.params.id});next(error);}});
router.post('/users',(req,res,next)=>{try{const{name,email}=req.body;if(!name||typeof name!=='string'||name.trim().length===0)return res.status(400).json({error:'Name is required and must be non-empty string'});if(!email||typeof email!=='string'||email.trim().length===0)return res.status(400).json({error:'Email is required and must be non-empty string'});const emailRegex=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;if(!emailRegex.test(email))return res.status(400).json({error:'Invalid email format'});if(users.some(u=>u.email===email))return res.status(400).json({error:'Email already exists'});const newUser={id:nextId++,name,email};users.push(newUser);res.status(201).json(newUser);}catch(error){qerrors(error,'users.post: creating new user',{name:req.body.name,email:req.body.email});next(error);}});
router.get('/users',(req,res,next)=>{try{res.json({users,count:users.length});}catch(error){qerrors(error,'users.get: retrieving all users',{userCount:users.length});next(error);}});
router.put('/users/:id',(req,res,next)=>{try{const{id}=req.params;const{name,email}=req.body;if(!/^[1-9]\d*$/.test(id))return res.status(400).json({error:'Invalid user ID format'});const userId=parseInt(id);const userIndex=users.findIndex(u=>u.id===userId);if(userIndex===-1)return res.status(404).json({error:'User not found'});if(name)users[userIndex].name=name;if(email){if(users.some(u=>u.email===email&&u.id!==userId))return res.status(400).json({error:'Email already exists'});users[userIndex].email=email;}res.json(users[userIndex]);}catch(error){qerrors(error,'users.put: updating user',{userId:req.params.id,name:req.body.name,email:req.body.email});next(error);}});
router.delete('/users/:id',(req,res,next)=>{try{const{id}=req.params;if(!/^[1-9]\d*$/.test(id))return res.status(400).json({error:'Invalid user ID format'});const userId=parseInt(id);const userIndex=users.findIndex(u=>u.id===userId);if(userIndex===-1)return res.status(404).json({error:'User not found'});const deletedUser=users.splice(userIndex,1)[0];res.json({message:'User deleted successfully',user:deletedUser});}catch(error){qerrors(error,'users.delete: deleting user',{userId:req.params.id});next(error);}});
module.exports=router;
