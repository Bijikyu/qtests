// Generated unit test for rateLimiter-flexible.ts - TypeScript ES module
// 🚩AI: ENTRY_POINT_FOR_GENERATED_TEST_IMPORTS
import 'qtests/setup';

let testModule: any;
beforeAll(async () => {
  testModule = await import('./rateLimiter-flexible');
});

describe('createDistributedRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['createDistributedRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('createInMemoryRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['createInMemoryRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('checkRateLimit', () => {
  it('is defined', () => {
    const target = (testModule as any)['checkRateLimit'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('resetRateLimitKey', () => {
  it('is defined', () => {
    const target = (testModule as any)['resetRateLimitKey'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('getRateLimitStats', () => {
  it('is defined', () => {
    const target = (testModule as any)['getRateLimitStats'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('getDistributedRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['getDistributedRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('getInMemoryRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['getInMemoryRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('DistributedRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['DistributedRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('InMemoryRateLimiter', () => {
  it('is defined', () => {
    const target = (testModule as any)['InMemoryRateLimiter'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('RateLimiterRedis', () => {
  it('is defined', () => {
    const target = (testModule as any)['RateLimiterRedis'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('RateLimiterMemory', () => {
  it('is defined', () => {
    const target = (testModule as any)['RateLimiterMemory'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});

describe('RateLimiterAbstract', () => {
  it('is defined', () => {
    const target = (testModule as any)['RateLimiterAbstract'];
    if (typeof target === 'undefined') {
      // Skip: export not found on module at runtime
      expect(true).toBe(true);
      return;
    }
    expect(target).toBeDefined();
  });
});
